version: "2"

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - gofmt

linters-settings:
  govet:
    enable-all: false

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
